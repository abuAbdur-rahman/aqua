# App Phase 4 - Activity Monitor

Read-only live CPU/mem/disk/process display over `WS /ws/sysmon` (daemon Phase 3, port 61234). Bounded chart history, sortable virtualized process table, formatters, and connection/error states.

## Design refs

- `DESIGN.md` + `UI-SPEC-00` + `UI-SPEC-04-ActivityMonitor.md` (three stat cards 8px radius, 60pt sparkline/bar, thresholds accent→warning→danger, states Loading/Populated/Disconnected).
