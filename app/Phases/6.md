# App Phase 6 - Spotlight

System-wide shortcut palette via Tauri global-shortcut, debounced `GET /api/search?q=` (daemon Phase 4, port 61234), grouped file/app/action results, keyboard/mouse selection, focus restore, and allowlisted `react-icons`.

## Design refs

- `DESIGN.md` + `UI-SPEC-00` + `UI-SPEC-06-Spotlight.md` (floating bg-overlay 10px radius upper-third, 180ms scale 0.96→1, groups Apps→Files→Actions, states Empty-query/Loading/No-results/Error, debounced 150–200ms).
