# App Phase 7 - Spaces and Mission Control

Multi-desktop, window migration, keyboard switching, and Mission Control thumbnails over the Phase 1 window store. No persistence yet (Phase 8).

## Design refs

- `UI-SPEC-01-Chrome.md` (window open from Dock rect) remains the chrome source; this phase adds Space switching without new chrome tokens.
